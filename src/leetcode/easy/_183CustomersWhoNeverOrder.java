package leetcode.easy;

public class _183CustomersWhoNeverOrder {
    //# Write your MySQL query statement below
    //
    //SELECT c.name Customers FROM Customers c
    //LEFT JOIN Orders o ON o.customerId = c.id
    //GROUP BY c.id
    //HAVING COUNT(o.id) = 0;
}
